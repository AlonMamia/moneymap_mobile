import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    title: {
        fontWeight: 'semibold',
        fontSize: 52,
        color: 'white',
    }
});

export default styles;
